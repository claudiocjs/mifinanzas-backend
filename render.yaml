services:
  - type: web
    name: mifinanzas-backend
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn v056:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
